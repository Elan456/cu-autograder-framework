// Includes the student's main file and calls a function defined within it to test it 
#include "studentMainNoMain.cpp"

int main(int argc, char *argv[]) {
    say_hello(); // Call the function defined in the student's main file, the test.py script will check the output
}